{{ form_start(form, {'action': path('task_file_new', { 'task_id': task_id }) }) }}

{{ form_row(form.file) }}

<div class="form-group">
  <div class="col-lg-offset-2 col-lg-10">
    <button type="submit" class="btn btn-primary">Upload</button>
  </div>
</div>

{{ form_end(form) }}
